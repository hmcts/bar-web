{% extends "layout.njk" %}

{% set title = t('Welcome to BARS') %}
{% set heading = t( title ) %}

{% block content %}

<div class="grid-row">
    <div class="column-full">
		<p class="lede">{{ message }}</p>
    </div>
</div>

{% endblock %}
